[package]
name = "probabilistic_llm_os"
version = "0.1.0"
edition = "2021"
description = "Kernel schedules based on expected information gain; high-entropy prompts get priority."
license = "MIT"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true

[dependencies]

[dependencies.bootloader]
version = "0.9"
features = ["map_physical_memory"]
