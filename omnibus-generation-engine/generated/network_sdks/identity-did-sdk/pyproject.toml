[project]
name = "identity_did_sdk"
version = "0.1.0"
description = "Self-sovereign identity with verifiable credentials and selective disclosure."
requires-python = ">=3.10"
dependencies = ["pydantic>=2.0", "anyio>=3.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
