[project]
name = "storage_dht_sdk"
version = "0.1.0"
description = "Mutable DHT with erasure-coded storage and proof-of-retrievability."
requires-python = ">=3.10"
dependencies = ["pydantic>=2.0", "anyio>=3.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
