[project]
name = "mesh_network_sdk"
version = "0.1.0"
description = "Self-healing mesh topology with OLSR-inspired routing for IoT and edge clusters."
requires-python = ">=3.10"
dependencies = ["pydantic>=2.0", "anyio>=3.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
